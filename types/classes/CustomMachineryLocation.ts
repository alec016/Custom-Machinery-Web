import ResourceLocation from './ResourceLocation'

class CustomMachineryLocation extends ResourceLocation {
  constructor (path: string) {
    super('custommachinery', path)
  }
}

export default CustomMachineryLocation
